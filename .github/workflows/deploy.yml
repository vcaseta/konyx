name: Build and Publish Version2

on:
  push:
    branches:
      - version2

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Clonar repositorio
      - name: Checkout repository
        uses: actions/checkout@v3

      # 2️⃣ Configurar Docker Buildx
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      # 3️⃣ Login a GHCR
      - name: Login to GHCR
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }} # Para usuario personal funciona

      # -------------------- Frontend --------------------
      - name: Build frontend image
        run: docker build -t ghcr.io/${{ github.repository_owner }}/konyx-frontend:version2 ./frontend

      - name: Push frontend image
        run: docker push ghcr.io/${{ github.repository_owner }}/konyx-frontend:version2

      # -------------------- Backend --------------------
      - name: Build backend image
        run: docker build -t ghcr.io/${{ github.repository_owner }}/konyx-backend:version2 ./backend

      - name: Push backend image
        run: docker push ghcr.io/${{ github.repository_owner }}/konyx-backend:version2

